import React,{useRef,useState} from 'react'; import {createRoot} from 'react-dom/client'; import {Camera,MapPin,ShieldCheck,AlertTriangle,RefreshCw} from 'lucide-react'; import './styles.css';
const API=import.meta.env.VITE_API_URL||'http://localhost:3000';
type Match={project:{id:string;name:string;description:string;agency:string;contractor?:string;budget?:number;status:string;progress?:number;location:string;sourceUrl:string;lastChecked:string};confidence:number;evidence:string[]};
function App(){const input=useRef<HTMLInputElement>(null);const [file,setFile]=useState<File>();const [coords,setCoords]=useState<{latitude:number;longitude:number}>();const [loading,setLoading]=useState(false);const [result,setResult]=useState<{status:string;analysis?:any;matches?:Match[]}>();const [reports,setReports]=useState<any[]>([]);const locate=()=>navigator.geolocation.getCurrentPosition(p=>setCoords({latitude:p.coords.latitude,longitude:p.coords.longitude}),()=>alert('Location is needed to match nearby projects.')); const scan=async()=>{if(!file||!coords)return;setLoading(true);const fd=new FormData();fd.append('file',file);fd.append('latitude',String(coords.latitude));fd.append('longitude',String(coords.longitude));const r=await fetch(`${API}/api/scans`,{method:'POST',body:fd});setResult(await r.json());setLoading(false)}; const loadReports=async()=>{const r=await fetch(`${API}/api/reports`);setReports((await r.json()).items)}; return <main><header><div className="brand"><ShieldCheck/> CivicLens</div></header><section className="hero"><p className="eyebrow">TRANSPARENCY, IN YOUR HANDS</p><h1>See what your city is building.</h1><p>Photograph public infrastructure in Cebu City and connect it to verified project records.</p><div className="actions"><button className="primary" onClick={()=>input.current?.click()}><Camera/> Take a project photo</button><button className="secondary" onClick={locate}><MapPin/> {coords?'Location ready':'Allow location'}</button></div><input ref={input} hidden type="file" accept="image/*" capture="environment" onChange={e=>{setFile(e.target.files?.[0]);setResult(undefined)}}/></section>{file&&<section className="card"><img className="preview" src={URL.createObjectURL(file)}/><div><h2>{file.name}</h2><p className="muted">{coords?'Ready to analyze with location evidence.':'Allow location to continue.'}</p><button className="primary" disabled={!coords||loading} onClick={scan}>{loading?<><RefreshCw className="spin"/> Analyzing…</>:<>Identify this project</>}</button></div></section>}{result&&<section className="results"><h2>{result.status==='needs_retake'?'We need a clearer photo':'Possible official projects'}</h2>{result.status==='needs_retake'?<div className="notice"><AlertTriangle/><p>We couldn’t confidently connect this image to an official record. Capture the project signboard or a wider, clearer angle and try again.</p></div>:result.matches?.map(m=><article className="project" key={m.project.id}><div><span className="tag">{Math.round(m.confidence*100)}% match</span><h3>{m.project.name}</h3><p>{m.project.description}</p><p className="muted">{m.project.location} · {m.project.status} · {m.project.agency}</p><p className="evidence">{m.evidence.join(' · ')}</p><a href={m.project.sourceUrl} target="_blank">View official source ↗</a></div><div className="progress"><strong>{m.project.progress??'—'}{m.project.progress?'%':''}</strong><small>reported progress</small></div></article>)}</section>}<section className="feed"><div className="feed-title"><div><p className="eyebrow">COMMUNITY WATCH</p><h2>Anomaly reports</h2></div><button className="secondary" onClick={loadReports}>Refresh feed</button></div>{reports.length===0?<p className="muted">Reports from residents will appear here.</p>:reports.map(r=><article className="report" key={r.id}><AlertTriangle/><div><strong>{r.category}</strong><p>{r.note}</p><small>Reported by {r.authorName} · {new Date(r.createdAt).toLocaleDateString()} · Unverified</small></div></article>)}</section><footer>Official records are shown with source attribution. AI helps find candidates; it does not verify government facts.</footer></main>};createRoot(document.getElementById('root')!).render(<App/>);
